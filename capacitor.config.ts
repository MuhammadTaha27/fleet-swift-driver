import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fleetManagementSystem.FleetSwiftDriver',
  appName: 'FleetSwiftDriver',
  webDir: 'dist'
};

export default config;
